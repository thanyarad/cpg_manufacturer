resources:
  pipelines:
    product_bronze:
      name: product_bronze
      libraries:
        - glob:
            include: ../../../src/product/product_bronze/transformations/**
      serverless: true
      catalog: ${var.catalog}
      schema: ${var.landing}
      root_path: ../../../src/product/product_bronze
      configuration:
        catalog: ${var.catalog}
        pipeline_schema: ${var.landing}
        target_schema: ${var.bronze}
        volume: ${var.input_volume}
