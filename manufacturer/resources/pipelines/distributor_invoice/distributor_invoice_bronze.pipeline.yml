resources:
  pipelines:
    distributor_invoice_bronze:
      name: distributor_invoice_bronze
      libraries:
        - glob:
            include: ../../../src/distributor_invoice/distributor_invoice_bronze/transformations/**
      serverless: true
      catalog: ${var.catalog}
      schema: ${var.landing}
      root_path: ../../../src/distributor_invoice/distributor_invoice_bronze
      configuration:
        catalog: ${var.catalog}
        pipeline_schema: ${var.landing}
        target_schema: ${var.bronze}
        volume: ${var.input_volume}
# /Workspace/Users/mugunthan2783@gmail.com/cpg_manufacturer/manufacturer/src/distributor_invoice/distributor_invoice_bronze/transformations
