resources:
  pipelines:
    consumer_bronze:
      name: consumer_bronze
      libraries:
        - glob:
            include: ../../../src/consumer/consumer_bronze/transformations/**
      serverless: true
      catalog: ${var.catalog}
      schema: ${var.landing}
      root_path: ../../../src/consumer/consumer_bronze
      configuration:
        catalog: ${var.catalog}
        pipeline_schema: ${var.landing}
        target_schema: ${var.bronze}
        volume: ${var.input_volume}