resources:
  pipelines:
    consumer_orders_silver:
      name: consumer_orders_silver
      libraries:
        - glob:
            include: ../../../src/consumer_orders/consumer_orders_silver/transformations/**
      serverless: true
      catalog: ${var.catalog}
      schema: ${var.bronze}
      root_path: ../../../src/consumer_orders/consumer_orders_silver
      configuration:
        catalog: ${var.catalog}
        target_schema: ${var.silver}

