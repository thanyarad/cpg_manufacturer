resources:
  pipelines:
    consumer_order_items_bronze:
      name: consumer_order_items_bronze
      libraries:
        - glob:
            include: ../../../src/consumer_orders/consumer_order_items_bronze/transformations/**
      serverless: true
      catalog: ${var.catalog}
      schema: ${var.landing}
      root_path: ../../../src/consumer_orders/consumer_order_items_bronze
      configuration:
        catalog: ${var.catalog}
        pipeline_schema: ${var.landing}
        target_schema: ${var.bronze}
        volume: ${var.input_volume}

