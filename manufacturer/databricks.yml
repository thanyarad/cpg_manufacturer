# This is a Databricks asset bundle definition for cpg_manufacturer.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: manufacturer
  uuid: 0cddd31f-0c85-4c6e-93e6-195970861549

include:
  - resources/*.yml
  - resources/**/*.yml

artifacts:
  python_artifact:
    type: whl
    build: uv build --wheel

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      # host: ${workspace.host}
      # host: https://dbc-dcffaa68-27b1.cloud.databricks.com
    variables:
      catalog: dev
      schema: dev
  prod:
    mode: production
    workspace:
      # host: https://dbc-dcffaa68-27b1.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/thanyarkottary2003@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/${workspace.current_user.userName}/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: dev
      schema: prod
    permissions:
      - user_name: ${workspace.current_user.userName}
        level: CAN_MANAGE
